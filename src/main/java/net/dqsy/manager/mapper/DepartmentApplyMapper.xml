<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.dqsy.manager.mapper.DepartmentApplyMapper">

    <insert id="insert">
        INSERT INTO department_apply
        (account_id, department_id, type, content, status, create_time) VALUES (#{apply.accountId, #{apply.departmentId}, #{apply.type}, #{apply.content}, #{apply.status}, #{apply.createTime})
    </insert>

</mapper>